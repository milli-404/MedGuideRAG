# --- Data extraction ---
datasets==2.20.0          # Hugging Face datasets library, used to download the medical corpus

# --- RAG framework ---
langchain==0.2.16
langchain-community==0.2.16
langchain-huggingface==0.0.3   # wraps sentence-transformers embeddings for LangChain
langchain-chroma==0.1.3        # LangChain <-> Chroma vector DB integration

# --- Embeddings (open-source, run locally, free) ---
sentence-transformers==3.0.1

# --- Vector database (local, file-based, free) ---
chromadb>=0.5.0,<0.6.0    # avoid exact ==0.5.5, it conflicts with langchain-chroma

# --- LLM API client (cloud, free tier available) ---
httpx<0.28                # groq/openai clients break on httpx>=0.28's removed "proxies" arg
groq==0.11.0              # fast, free-tier API for open-source LLMs (Llama 3, Mixtral, etc.)
openai==1.44.0            # optional: swap in OpenAI/GPT models instead of Groq

# --- Utilities ---
python-dotenv==1.0.1       # loads API keys from a .env file instead of hardcoding them
tqdm==4.66.5

# --- Optional: simple web UI ---
streamlit==1.38.0